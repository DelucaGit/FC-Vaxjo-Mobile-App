---
description: After each git push, add new LinkedIn post ideas on top of the local list
alwaysApply: true
---

# LinkedIn post ideas (`LK_POSTS.md`)

## When this runs
After a **successful `git push`** (including the automatic milestone push), add new LinkedIn ideas.

## Steps
1. Read `logs/Developer Log of FC Växjö Mobile App.md` (latest entries matter most).
2. Read the current `LK_POSTS.md` so old ideas are not lost.
3. Write **at most 10 new** post ideas from **this push** (fewer is better if quality would drop; do not pad).
4. **Add them on top.** Keep every older idea below. Do **not** replace or rewrite the whole file.
5. Skip an idea if it is already in the file (same lesson, even if the wording differs).
6. Keep a short “last updated” line at the top. Newest date section first.

## Quality bar (IT recruiters)
Each idea must show at least one of: **passion for building**, **how a problem was solved**, **how the project is managed**, **how he communicates** (teachers, stakeholders, AI, club).

Skip: jokes with no lesson, raw screenshots-only notes, “I installed Postgres,” or anything that leaks secrets, personal family drama, or real people’s private data.

Write ideas he could actually post: short hook + what happened + what it shows. First person is OK. Simple English.

## Git
`LK_POSTS.md` is **local only**. It is in `.gitignore`. Never `git add` it. Never commit or push it. If it appears in `git status` as untracked, leave it untracked.
